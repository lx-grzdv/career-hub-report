# 🔍 Диагностика проблем с доступом из РФ

## Текущий продакшен

**Основной домен:** https://career-hub-report.vercel.app (Vercel). Этот домен не блокируется в РФ.

Если вы открываете сайт по другой ссылке (например, Figma Make):

### ⚠️ Домен figma.site

Домен `*.figma.site` и `figma.com` **могут быть заблокированы** интернет-провайдерами в России и СНГ. Это не проблема кода — блокировка на уровне провайдера/государства.

**Решение:** используйте ссылку на Vercel (career-hub-report.vercel.app) или см. [ALTERNATIVE_HOSTING.md](/ALTERNATIVE_HOSTING.md).

## ⚠️ Возможные причины блокировки

### 1. **Домен Figma заблокирован**
Если сайт хостится на `figma.com` или `make.figma.com`, он может быть заблокирован в РФ.

**Решение:**
- Экспортируйте приложение и разместите на собственном хостинге (Vercel, Netlify, GitHub Pages)
- Используйте собственный домен

### 2. **Telegram WebView ограничения**
Если вы открываете сайт через Telegram WebView (внутренний браузер Telegram):

**Проверка:**
- Откройте консоль разработчика (если возможно)
- Проверьте User-Agent: содержит ли "Telegram"
- Попробуйте открыть ссылку во внешнем браузере (Safari/Chrome)

**Решение:**
- Добавьте кнопку "Открыть во внешнем браузере"
- Используйте атрибут `target="_blank"` для всех внешних ссылок

### 3. **DNS prefetch блокировка**
Браузеры автоматически делают DNS prefetch для всех ссылок на странице.

**Статус:** ✅ **ИСПРАВЛЕНО**
- Добавлен meta тег `x-dns-prefetch-control: off`
- Все внешние запросы отключены
- Ссылки на t.me не делают prefetch

## 🔧 Текущие оптимизации

### Что уже сделано:
- ✅ Убран DNS prefetch к t.me
- ✅ Убран Service Worker
- ✅ Убран PWA manifest
- ✅ Убраны Web Vitals метрики
- ✅ Добавлен x-dns-prefetch-control
- ✅ Все ресурсы локальные (без CDN)
- ✅ Нет внешних библиотек/fonts
- ✅ Нет Analytics/tracking

### Как это работает:
```typescript
// Отключаем автоматический DNS prefetch
<meta http-equiv="x-dns-prefetch-control" content="off">

// Ссылки на Telegram открываются только по клику
<a href="https://t.me/channel" rel="noopener noreferrer" target="_blank">

// Определяем Telegram WebView
const isTelegramWebView = navigator.userAgent.includes('Telegram');
```

## 📱 Тестирование

### Как проверить что блокируется:

1. **Откройте консоль браузера** (F12)
2. **Перейдите на вкладку Network**
3. **Обновите страницу**
4. **Проверьте:**
   - Есть ли запросы с ошибками (красные)
   - Есть ли запросы к внешним доменам
   - Какие ресурсы не загружаются

### Команды для проверки:

```javascript
// В консоли браузера:

// 1. Проверить User-Agent
console.log(navigator.userAgent);

// 2. Проверить все мета-теги
Array.from(document.querySelectorAll('meta')).forEach(meta => {
  console.log(meta.name || meta.httpEquiv, ':', meta.content);
});

// 3. Проверить все внешние ссылки
Array.from(document.querySelectorAll('a[href*="://"]')).forEach(link => {
  console.log(link.href);
});

// 4. Проверить загруженные скрипты
Array.from(document.querySelectorAll('script[src]')).forEach(script => {
  console.log(script.src);
});
```

## 🌍 Альтернативные решения

### Если проблема в домене Figma:

1. **Экспорт приложения:**
   ```bash
   npm run build
   # Папка dist/ содержит статические файлы
   ```

2. **Размещение на Vercel:**
   - Создайте аккаунт на vercel.com
   - Загрузите папку dist/
   - Получите свой домен: `your-app.vercel.app`

3. **Размещение на GitHub Pages:**
   - Создайте репозиторий на GitHub
   - Загрузите файлы из dist/
   - Включите GitHub Pages в настройках
   - Доступ: `username.github.io/repo`

4. **Размещение на Netlify:**
   - Создайте аккаунт на netlify.com
   - Drag & drop папку dist/
   - Получите домен: `your-app.netlify.app`

## 🚀 Быстрая проверка

Если сайт **НЕ** открывается без VPN:

1. ✅ Попробуйте открыть во внешнем браузере (не Telegram)
2. ✅ Проверьте консоль на ошибки
3. ✅ Проверьте Network tab на заблокированные запросы
4. ✅ Попробуйте другой браузер (Chrome, Safari, Firefox)

Если сайт **ОТКРЫВАЕТСЯ** в обычном браузере, но **НЕ** в Telegram:
- Проблема в Telegram WebView
- Используйте кнопку "Открыть в браузере"

Если сайт **НЕ ОТКРЫВАЕТСЯ** вообще:
- Проблема в домене Figma
- Необходим собственный хостинг

## 📞 Поддержка

Если проблема остаётся:
1. Откройте консоль браузера (F12)
2. Сделайте скриншот вкладки Console
3. Сделайте скриншот вкладки Network
4. Отправьте информацию для диагностики